import {StyleSheet} from 'react-native';
import { Colors } from '../Theme';

export default StyleSheet.create({
    standardCircle: {
        width: 40,
        height: 40,
        borderRadius: 20,
    }
})