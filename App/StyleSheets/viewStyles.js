import {StyleSheet} from 'react-native';
import { Colors } from '../Theme';

export default StyleSheet.create({
    standardCircle: {
        width: 50,
        height: 50,
        borderRadius: 25,
    }
})